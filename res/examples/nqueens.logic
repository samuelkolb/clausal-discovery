type index > int

// position(queen, column, row)
pred position(index, index, index)

// gap(index1, index2, distance)
pred gap(index, index, index)

example 1 {
	const index 1 2 3 4

	position(1, 1, 2)
	position(2, 2, 4)
	position(3, 3, 1)
	position(4, 4, 3)
}