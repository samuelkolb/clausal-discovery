type Area
type Person
// type Cost > int

pred low_crime(Area)
pred school(Person, Area)
pred work(Person, Area)

example 1 {
	const Area A1 A2
	const Person P

	school(P, A1)
	low_crime(A1)

	work(P, A2)
}

example 2 {
	const Area A1 A2
	const Person P

	school(P, A1)
	low_crime(A1)

	work(P, A2)
	low_crime(A2)
}

example 3 {
	const Area A1 A2
	const Person P

	school(P, A1)
	work(P, A1)
}