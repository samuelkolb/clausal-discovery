type Area
type Person

pred low_crime(Area)
pred school(Person, Area)
pred work(Person, Area)

example {
	const Area A1 A2
	const Person P

	school(P, A1)
	low_crime(A1)

	work(P, A2)
}

example {
	const Area A1 A2
	const Person P

	school(P, A1)
	low_crime(A1)

	work(P, A2)
	low_crime(A2)
}

example {
	const Area A1 A2
	const Person P

	school(P, A1)
	work(P, A1)
}

pref 1 > 2
pref 2 > 3